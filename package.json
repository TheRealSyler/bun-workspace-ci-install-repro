{
  "name": "bun-workspace-ci-install-repro",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run --elide-lines 0 --workspaces test",
    "test:ci": "bun run --workspaces test:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "vitest": "latest"
  }
}